# Simple web app for exiting unix cronjobs

Uses the default crontab, make sure it's installed and setup

## How to install?

1. Go to GitHub release and download the compatible version for your operating
   system.

2. Make sure it's has executable permission

```sh
chmod +x ./cronweb
```

3. Move the binary to `/usr/bin/`
